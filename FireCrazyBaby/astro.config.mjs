import { defineConfig } from "astro/config";

export default defineConfig({
  // Optional: once your site is live on Netlify, you can replace this with
  // your real site URL (e.g. "https://your-site-name.netlify.app").
  // It's only used for things like canonical links — the site works fine
  // without changing it.
  site: "https://example.netlify.app",
});
